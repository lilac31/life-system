# 调试窗口更新说明

## 📋 更新内容

已将原来的 GitHub 调试窗口更新为 JSONBin 专用调试窗口，并增强了云端数据展示功能。

## 🎨 主要改进

### 1. **界面更新**
- ✅ 标题改为 "JSONBin 同步调试器"
- ✅ 边框颜色改为紫色（JSONBin 品牌色）
- ✅ 按钮颜色统一为紫色主题

### 2. **显示信息调整**
- ❌ 移除：GitHub Token、GitHub用户名、Gist ID、API速率限制
- ✅ 新增：JSONBin API Key状态、Bin ID
- ✅ 保留：用户ID、本地数据、同步状态、最后同步时间

### 3. **云端数据展示** 🆕
点击 "测试下载" 后，会显示云端数据的详细摘要：
- 周重要任务数量
- 快速任务天数
- 时间记录条数
- 周数据数量
- 重要任务个数
- 总工作时间
- 完整 JSON 数据（可折叠查看）

## 🖥️ 使用方式

### 打开调试窗口
调试窗口默认显示在右下角，包含以下功能：

### 1️⃣ 测试上传同步
- 点击 "🧪 测试上传同步" 按钮
- 会将当前所有本地数据上传到 JSONBin
- 显示上传结果（Bin ID、上传时间、数据字段）

### 2️⃣ 测试下载
- 点击 "📥 测试下载" 按钮
- 从 JSONBin 下载云端数据
- 自动显示数据摘要和完整内容

### 3️⃣ 云端数据摘要
下载成功后会显示：
```
☁️ 云端数据摘要
周重要任务: X 周
快速任务: X 天
时间记录: X 条
周数据: X 周
重要任务: X 个
总工作时间: Xh

▶ 查看完整数据 JSON
  [可展开查看完整 JSON]
```

### 4️⃣ 其他功能
- 📦 导入/导出数据
- 🗑️ 清除所有数据

## 📊 数据结构展示

云端数据完整结构：
```json
{
  "weeklyImportantTasks": {
    "2025-11-18": [...]
  },
  "quickTasks": {
    "2025-11-18": {
      "morning": [...],
      "noon": [...],
      "afternoon": [...],
      "evening": [...]
    }
  },
  "taskTimeRecords": {
    "task-id-1": 60,
    "task-id-2": 120
  },
  "weeks": {},
  "importantTasks": [],
  "totalWorkingHours": 40
}
```

## 🎯 使用场景

### 调试同步问题
1. 打开调试窗口
2. 查看 API Key 配置状态
3. 点击 "测试上传同步"
4. 检查控制台日志

### 验证数据完整性
1. 点击 "测试下载"
2. 查看云端数据摘要
3. 展开完整 JSON 对比本地数据
4. 确认所有字段都已同步

### 多设备同步验证
1. 设备A：上传数据
2. 设备B：配置相同 API Key
3. 设备B：点击 "测试下载"
4. 对比数据摘要确认同步成功

## 💡 提示信息

窗口底部显示：
```
💡 提示：
1. 先点击"测试上传同步"验证功能
2. 在另一个浏览器用相同API Key测试
3. 打开控制台查看详细日志
```

## 🔧 技术细节

### 数据摘要计算
```javascript
const getDataSummary = (data) => ({
  weeklyImportantTasks: Object.keys(data.weeklyImportantTasks || {}).length,
  quickTasks: Object.keys(data.quickTasks || {}).length,
  taskTimeRecords: Object.keys(data.taskTimeRecords || {}).length,
  weeks: Object.keys(data.weeks || {}).length,
  importantTasks: (data.importantTasks || []).length,
  totalWorkingHours: data.totalWorkingHours || 0
});
```

### 测试流程
1. **上传测试**：
   - 获取完整本地数据
   - 调用 `uploadToCloud()`
   - 验证上传结果

2. **下载测试**：
   - 调用 `downloadFromCloud()`
   - 解析云端数据
   - 生成摘要并展示

## 🎨 样式特点

- 紫色主题（#8B5CF6）
- 响应式设计
- 暗色模式支持
- 可折叠的详细信息
- 滚动条支持（最大高度 80vh）

## 📝 相关文件

- `src/components/SyncDebugger.jsx` - 调试窗口组件
- `src/components/SyncStatus.jsx` - 同步状态组件（也已更新）
- `src/services/apiService.js` - JSONBin API 服务

## ⚠️ 注意事项

1. 调试窗口始终显示，方便开发调试
2. 生产环境可通过快捷键切换显示（Ctrl+Shift+D）
3. 完整 JSON 数据较大时会自动滚动
4. 控制台会输出详细的同步日志
