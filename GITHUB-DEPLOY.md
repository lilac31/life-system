# 个人日程管理系统 - GitHub Pages 部署指南

## 🚀 简单的GitHub部署方案

### 方法一：使用GitHub Actions（推荐）

#### 1. 创建GitHub仓库
1. 登录 [GitHub](https://github.com)
2. 点击右上角的 "+" 号，选择 "New repository"
3. 填写仓库名称（建议：`life-system`）
4. 选择 "Public" 或 "Private"
5. 勾选 "Add a README file"
6. 点击 "Create repository"

#### 2. 上传代码到GitHub

```bash
# 如果还没有Git仓库，初始化一个
git init
git add .
git commit -m "Initial commit: 生活管理系统"

# 添加远程仓库（替换为你的GitHub用户名）
git remote add origin https://github.com/your-username/life-system.git
git branch -M main
git push -u origin main
```

#### 3. 启用GitHub Pages

1. 进入你的GitHub仓库
2. 点击 "Settings" 选项卡
3. 在左侧菜单中选择 "Pages"
4. 在 "Build and deployment" 部分：
   - Source: 选择 "GitHub Actions"
5. GitHub会自动运行部署流程

#### 4. 等待部署完成

1. 在 "Actions" 选项卡中查看部署进度
2. 部署完成后，在 "Pages" 设置中会显示访问地址
3. 访问地址格式为：`https://your-username.github.io/life-system`

#### 5. 自动更新

每次推送代码到 `main` 分支，GitHub会自动重新部署网站！

### 方法二：手动部署（简单快速）

#### 1. 本地构建GitHub版本

```bash
npm run build:github
```

#### 2. 安装gh-pages工具

```bash
npm install -g gh-pages
```

#### 3. 部署到GitHub Pages

```bash
npm run deploy
```

## 🔧 自定义域名（可选）

### 1. 在GitHub仓库设置中添加域名

1. 进入仓库的 "Settings" → "Pages"
2. 在 "Custom domain" 中输入你的域名
3. 点击 "Save"

### 2. 配置DNS解析

在域名提供商处添加CNAME记录：

```
记录类型: CNAME
主机记录: www (或你想要的子域名)
记录值: your-username.github.io
TTL: 600
```

### 3. HTTPS证书

GitHub会自动为自定义域名提供HTTPS证书，无需手动配置。

## 📊 功能特点

✅ **完全免费**：GitHub Pages提供免费的静态网站托管
✅ **自动部署**：使用GitHub Actions自动部署
✅ **HTTPS支持**：自动配置SSL证书
✅ **自定义域名**：支持绑定自己的域名
✅ **快速访问**：全球CDN加速，访问速度快
✅ **版本控制**：每次部署都有版本记录

## 🔄 更新应用

修改代码后，只需：

```bash
git add .
git commit -m "更新功能描述"
git push origin main
```

GitHub会自动重新部署最新版本！

## 📱 移动端访问

应用已针对移动端优化，在手机和平板电脑上都能完美使用。

## 💡 数据存储

应用数据存储在浏览器本地存储中，无需服务器或数据库。

## 🎯 访问你的应用

部署完成后，你可以通过以下地址访问：

- **GitHub Pages地址**：`https://your-username.github.io/life-system`
- **自定义域名**：如果配置了自定义域名

---

**完成！你的个人日程管理系统现在可以在全球范围内访问了！** 🎉